Victor-Jenkins
MASTER_Selenium_Allure

Type / to search
Code
Issues
Pull requests
Actions
Projects
Security
1
Insights
Settings
Build, Test, Generate Allure Report, and Deploy to gh-pages
Build, Test, Generate Allure Report, and Deploy to gh-pages #127
Jobs
Run details
Workflow file for this run
.github/workflows/main.yml at 2dee937
name: Build, Test, Generate Allure Report, and Deploy to gh-pages

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 0 * * 0' # Ejecutar cada domingo a la medianoche (UTC)

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'

    - name: Build and test with Maven
      run: mvn clean install

    - name: Generate Allure report
      run: mvn allure:report

    - name: Deploy Allure Report to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.SELENIUM }}
        publish_dir: ./target/results/allure-report
        publish_branch: gh-pages
